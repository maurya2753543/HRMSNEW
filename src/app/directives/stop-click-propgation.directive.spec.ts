import { StopClickPropgationDirective } from './stop-click-propgation.directive';

describe('StopClickPropgationDirective', () => {
  it('should create an instance', () => {
    const directive = new StopClickPropgationDirective();
    expect(directive).toBeTruthy();
  });
});
