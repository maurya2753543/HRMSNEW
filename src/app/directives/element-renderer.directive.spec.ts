import { ElementRendererDirective } from './element-renderer.directive';

describe('ElementRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementRendererDirective();
    expect(directive).toBeTruthy();
  });
});
